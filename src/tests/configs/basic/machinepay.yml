server:
  hostName: localhost
  http:
    enabled: true
    port: 8080
  https:
    enabled: true
    port: 8443
    certFile: certs/insecure_test_localhost.crt
    keyFile: secrets/insecure_test_localhost.key
log:
  level: info
network:
  facilitator:
    type: cdp
    baseUrl: https://api.coinbase.com/v2
    apiKeyFile: secrets/coinbase_api_key.txt
